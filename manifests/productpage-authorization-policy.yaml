apiVersion: "security.istio.io/v1beta1"
kind: "AuthorizationPolicy"
metadata:
  name: "productpage"
  namespace: default
spec:
  selector:
    matchLabels:
      app: productpage
  rules:
  - when:
    - key: request.headers[hello]
      values: ["world"]
